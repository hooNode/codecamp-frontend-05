import React from "react";

export default function PDetailPage() {
  return <div>Pcreate</div>;
}

// {item.images.length > 0 ? (
//   <>
//     {item.images.map((image) => (
//       <>
//         {image.endsWith(".png" || ".jpeg") ? (
//           <>
//             <img
//               id={image}
//               src={`https://storage.googleapis.com/${image}`}
//               width="78px"
//               height="78px"
//             />
//           </>
//         ) : (
//           <></>
//         )}
//       </>
//     ))}
//   </>
// ) : (
//   <></>
// )}
